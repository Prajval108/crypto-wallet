<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background-color: #00796b;
            color: #ffffff;
            text-align: center;
            padding: 30px 20px;
            font-size: 24px;
            font-weight: 600;
        }
        .content {
            padding: 20px;
            color: #333333;
            font-size: 16px;
            line-height: 1.5;
        }
        .content p {
            margin: 0 0 10px;
        }
        .details {
            background-color: #f0f0f0;
            padding: 15px;
            border-radius: 5px;
            margin-top: 10px;
        }
        .details p {
            margin: 5px 0;
            font-family: monospace;
        }
        .warning {
            color: #ff0000;
            font-size: 14px;
            margin-top: 10px;
        }
        .button {
            display: inline-block;
            padding: 15px 30px;
            background-color: #00796b;
            color: #ffffff !important;
            text-decoration: none !important;
            font-size: 16px;
            border-radius: 5px;
        }
        .footer {
            padding: 20px;
            text-align: center;
            color: #888888;
            font-size: 14px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            {{app_name}}
        </div>
        <div class="content">
            <p>{{text1}} {{name}},</p>
            <p>{{text2}} {{app_name}}.</p>
            <p>{{text3}}</p>
            <div class="details">
                <p><strong>Account:</strong> {{account}}</p>
                <p><strong>Public Key:</strong> {{publicKey}}</p>
                <p><strong>Private Key:</strong> {{privateKey}}</p>
            </div>
            <div class="warning">
                <p>Please do not share your account information, such as your private key, with anyone.</p>
            </div>
        </div>
        <div class="footer">
            &copy; 2024 {{app_name}}. All rights reserved.
        </div>
    </div>
</body>

</html>
